{# templates/task/index.html.twig #}

<h1>My Task List</h1>

<ul>
    {% for task in tasks %}
        <li>{{ task.title }} - {% if task.isCompleted %}Completed{% else %}Not completed{% endif %}</li>
    {% endfor %}
</ul>

<a href="{{ path('task_new') }}">Create new Task</a>
